# frozen_string_literal: true

RSpec.describe TimeBuffer do
  it "has a version number" do
    expect(TimeBuffer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
